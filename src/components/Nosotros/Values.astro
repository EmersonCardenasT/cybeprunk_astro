---
// Values.astro — Valores · Minimal · Steel / Olive
---

<section id="valores" class="val-section">

  <div class="val-bg" aria-hidden="true">
    <div class="val-grain"></div>
    <div class="val-line val-line-top"></div>
    <div class="val-line val-line-bottom"></div>
  </div>

  <div class="val-inner">

    <!-- ══ HEADER ══ -->
    <div class="val-header" data-reveal>
      <div class="val-header-left">
        <p class="val-eyebrow">
          <span class="eyebrow-pip"></span>
          Lo que nos define
        </p>
        <h2 class="val-title">
          Nuestros<br/>
          <span class="title-outline">valores.</span>
        </h2>
      </div>
      <p class="val-sub">
        No son palabras en una pared. Son los principios que guían cada decisión, cada reparación y cada conversación con un cliente.
      </p>
    </div>

    <!-- ══ VALUES GRID ══ -->
    <div class="val-grid">

      <!-- 01 · Honestidad -->
      <div class="val-card" data-reveal data-delay="0">
        <div class="val-card-top">
          <span class="val-num">01</span>
          <div class="val-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3L3 8.5v7C3 21.5 7.8 25.5 14 27c6.2-1.5 11-5.5 11-11.5v-7z" stroke="currentColor" stroke-width="1.3" stroke-linejoin="round"/>
              <path d="M9 14l3.5 3.5L19 11" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
        </div>
        <h3 class="val-name">Honestidad</h3>
        <p class="val-desc">
          Decimos lo que hay. Si un equipo no se puede reparar, lo decimos. Si hay una opción más barata, la ofrecemos. Sin vueltas, sin sorpresas.
        </p>
        <div class="val-card-footer">
          <span class="val-tag">Transparencia total</span>
        </div>
        <div class="val-accent"></div>
      </div>

      <!-- 02 · Calidad -->
      <div class="val-card" data-reveal data-delay="1">
        <div class="val-card-top">
          <span class="val-num">02</span>
          <div class="val-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <path d="M14 3l2.8 6 6.5.9-4.7 4.5 1.1 6.5L14 18l-5.7 2.9 1.1-6.5L4.7 9.9l6.5-.9z" stroke="currentColor" stroke-width="1.3" stroke-linejoin="round"/>
              <circle cx="14" cy="13" r="2.5" stroke="currentColor" stroke-width="1" opacity="0.45"/>
            </svg>
          </div>
        </div>
        <h3 class="val-name">Calidad</h3>
        <p class="val-desc">
          Cada reparación se hace con repuestos originales o certificados. Cada producto pasa control de calidad antes de llegar a tus manos.
        </p>
        <div class="val-card-footer">
          <span class="val-tag">Repuestos certificados</span>
        </div>
        <div class="val-accent"></div>
      </div>

      <!-- 03 · Compromiso -->
      <div class="val-card" data-reveal data-delay="2">
        <div class="val-card-top">
          <span class="val-num">03</span>
          <div class="val-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <!-- Handshake -->
              <path d="M3 17l4-4 3 1 4-4 5 1 4-4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M7 21l-4-4M21 21l4-4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
              <path d="M7 21c2 2 5 3 7 3s5-1 7-3" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
            </svg>
          </div>
        </div>
        <h3 class="val-name">Compromiso</h3>
        <p class="val-desc">
          Nos hacemos cargo de lo que entregamos. Si algo falla dentro de la garantía, lo resolvemos sin costo y sin discusión. Así de simple.
        </p>
        <div class="val-card-footer">
          <span class="val-tag">Garantía sin letra chica</span>
        </div>
        <div class="val-accent"></div>
      </div>

      <!-- 04 · Respeto -->
      <div class="val-card" data-reveal data-delay="3">
        <div class="val-card-top">
          <span class="val-num">04</span>
          <div class="val-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <!-- Two people / community -->
              <circle cx="10" cy="10" r="4" stroke="currentColor" stroke-width="1.3"/>
              <path d="M3 24c0-3.9 3.1-7 7-7s7 3.1 7 7" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
              <circle cx="20" cy="10" r="3" stroke="currentColor" stroke-width="1.1" opacity="0.55"/>
              <path d="M23 24c0-2.8-1.3-5.2-3.3-6.7" stroke="currentColor" stroke-width="1.1" stroke-linecap="round" opacity="0.55"/>
            </svg>
          </div>
        </div>
        <h3 class="val-name">Respeto</h3>
        <p class="val-desc">
          Tratamos a cada cliente como queremos ser tratados. Sin condescendencia, sin tecnicismos innecesarios. Tu tiempo y tu plata merecen respeto.
        </p>
        <div class="val-card-footer">
          <span class="val-tag">Trato humano siempre</span>
        </div>
        <div class="val-accent"></div>
      </div>

      <!-- 05 · Innovación -->
      <div class="val-card" data-reveal data-delay="4">
        <div class="val-card-top">
          <span class="val-num">05</span>
          <div class="val-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <!-- Lightbulb -->
              <path d="M14 4a7 7 0 0 1 5 12l-1 2H10l-1-2A7 7 0 0 1 14 4z" stroke="currentColor" stroke-width="1.3" stroke-linejoin="round"/>
              <line x1="11" y1="21" x2="17" y2="21" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
              <line x1="12" y1="24" x2="16" y2="24" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
              <line x1="14" y1="7"  x2="14" y2="11" stroke="currentColor" stroke-width="1" stroke-linecap="round" opacity="0.4"/>
            </svg>
          </div>
        </div>
        <h3 class="val-name">Innovación</h3>
        <p class="val-desc">
          Nos mantenemos al día con la tecnología para poder asesorarte mejor. Incorporamos nuevas técnicas de reparación y herramientas constantemente.
        </p>
        <div class="val-card-footer">
          <span class="val-tag">Siempre actualizados</span>
        </div>
        <div class="val-accent"></div>
      </div>

      <!-- 06 · Privacidad -->
      <div class="val-card" data-reveal data-delay="5">
        <div class="val-card-top">
          <span class="val-num">06</span>
          <div class="val-icon">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none">
              <!-- Lock -->
              <rect x="6" y="13" width="16" height="11" rx="2" stroke="currentColor" stroke-width="1.3"/>
              <path d="M9 13V9a5 5 0 0 1 10 0v4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
              <circle cx="14" cy="18.5" r="1.8" stroke="currentColor" stroke-width="1.1" opacity="0.5"/>
            </svg>
          </div>
        </div>
        <h3 class="val-name">Privacidad</h3>
        <p class="val-desc">
          Nunca accedemos a tus datos, fotos ni contraseñas. Tu equipo entra a reparación y sale igual que como llegó, solo que funcionando mejor.
        </p>
        <div class="val-card-footer">
          <span class="val-tag">Datos 100% seguros</span>
        </div>
        <div class="val-accent"></div>
      </div>

    </div><!-- /val-grid -->

    <!-- ══ BOTTOM CTA STRIP ══ -->
    <div class="val-cta-strip" data-reveal>
      <div class="val-cta-text">
        <span class="val-cta-label">¿Querés conocernos en persona?</span>
        <span class="val-cta-sub">Visitanos en Av. Corrientes 1234, CABA · Lun–Sáb 9 a 20 h</span>
      </div>
      <div class="val-cta-actions">
        <a href="#contacto" class="val-cta-btn val-cta-primary">
          Contactarnos
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
            <path d="M3 7h8M8 4l3 3-3 3" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <a href="#productos" class="val-cta-btn val-cta-ghost">Ver productos</a>
      </div>
    </div>

  </div>
</section>

<style>
  :root {
    --steel:    #4a6278;
    --steel-lt: #7a98ae;
    --olive:    #6b7c55;
    --olive-lt: #8fa070;
    --white:    #f4f3ef;
    --black:    #0a0a0a;
  }

  .val-section {
    --bg:      #f4f3ef; --bg-card: #ffffff;
    --text:    #0a0a0a; --muted: rgba(10,10,10,0.42);
    --border:  rgba(10,10,10,0.08);
    background: var(--bg); color: var(--text);
    position: relative; padding: 120px 0 100px;
    font-family: 'DM Sans', 'Helvetica Neue', sans-serif;
    overflow: hidden; transition: background 0.4s, color 0.4s;
  }
  :root.dark .val-section {
    --bg: #0c0c0c; --bg-card: #111111;
    --text: #f4f3ef; --muted: rgba(244,243,239,0.38);
    --border: rgba(244,243,239,0.07);
  }
  :root.dark .title-outline { -webkit-text-stroke: 1px rgba(244,243,239,0.28) !important; }

  .val-bg { position: absolute; inset: 0; pointer-events: none; }
  .val-grain {
    position: absolute; inset: 0; opacity: 0.025;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='g'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23g)'/%3E%3C/svg%3E");
    background-size: 256px;
  }
  .val-line { position: absolute; left: 0; right: 0; height: 1px; background: var(--border); }
  .val-line-top { top: 0; } .val-line-bottom { bottom: 0; }

  .val-inner {
    max-width: 1280px; margin: 0 auto; padding: 0 44px;
    display: flex; flex-direction: column; gap: 56px;
    position: relative; z-index: 1;
  }
  @media (max-width: 640px) { .val-inner { padding: 0 20px; gap: 40px; } }

  /* Header */
  .val-header {
    display: flex; align-items: flex-end;
    justify-content: space-between; gap: 40px; flex-wrap: wrap;
  }
  .val-header-left { display: flex; flex-direction: column; gap: 16px; }
  .val-eyebrow {
    display: flex; align-items: center; gap: 10px;
    font-size: 0.65rem; letter-spacing: 0.26em;
    text-transform: uppercase; color: var(--steel-lt); margin: 0;
  }
  .eyebrow-pip {
    width: 7px; height: 7px; background: var(--olive-lt);
    border-radius: 50%; box-shadow: 0 0 8px var(--olive); flex-shrink: 0;
  }
  .val-title {
    font-family: 'DM Serif Display', Georgia, serif;
    font-size: clamp(2.4rem, 5vw, 4rem);
    font-weight: 400; line-height: 0.93;
    letter-spacing: -0.035em; color: var(--text); margin: 0;
  }
  .title-outline { color: transparent; -webkit-text-stroke: 1px rgba(10,10,10,0.35); }
  .val-sub {
    font-size: 0.88rem; color: var(--muted); line-height: 1.75;
    margin: 0; font-weight: 300; max-width: 380px; flex-shrink: 0;
    align-self: flex-end;
  }

  /* Grid 3×2 */
  .val-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
  }
  @media (max-width: 860px) { .val-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 500px) { .val-grid { grid-template-columns: 1fr; } }

  /* Card */
  .val-card {
    background: var(--bg-card);
    position: relative; overflow: hidden;
    display: flex; flex-direction: column;
    transition: background 0.3s;
  }
  .val-card:hover {
    background: color-mix(in srgb, var(--bg-card), var(--steel) 3%);
    z-index: 1;
  }
  :root.dark .val-card:hover { background: rgba(74,98,120,0.06); }

  .val-card-top {
    display: flex; align-items: flex-start;
    justify-content: space-between;
    padding: 28px 28px 0;
  }

  .val-num {
    font-size: 0.55rem; font-weight: 600;
    letter-spacing: 0.22em; color: var(--muted); opacity: 0.45;
    font-variant-numeric: tabular-nums;
    transition: opacity 0.25s, color 0.25s;
  }
  .val-card:hover .val-num { opacity: 0.9; color: var(--steel-lt); }

  .val-icon {
    width: 50px; height: 50px;
    display: flex; align-items: center; justify-content: center;
    border: 1px solid var(--border);
    color: var(--muted);
    background: var(--bg);
    transition: border-color 0.35s, color 0.35s, box-shadow 0.35s, transform 0.35s, background 0.35s;
  }
  .val-card:hover .val-icon {
    border-color: var(--steel);
    color: var(--steel-lt);
    background: rgba(74,98,120,0.05);
    box-shadow: 0 0 18px rgba(74,98,120,0.14), inset 0 0 10px rgba(74,98,120,0.04);
    transform: translateY(-3px);
  }

  .val-name {
    font-family: 'DM Serif Display', serif;
    font-size: 1.3rem; font-weight: 400;
    letter-spacing: -0.02em; color: var(--text); margin: 0;
    padding: 18px 28px 0;
    transition: color 0.25s;
  }

  .val-desc {
    font-size: 0.8rem; color: var(--muted); line-height: 1.8;
    font-weight: 300; margin: 0;
    padding: 10px 28px 0; flex: 1;
  }

  .val-card-footer {
    padding: 18px 28px 28px;
  }
  .val-tag {
    display: inline-flex; align-items: center;
    font-size: 0.55rem; font-weight: 600;
    letter-spacing: 0.18em; text-transform: uppercase;
    color: var(--olive-lt);
    background: rgba(107,124,85,0.08);
    border: 1px solid rgba(107,124,85,0.2);
    padding: 3px 10px;
    transition: background 0.25s, border-color 0.25s;
  }
  .val-card:hover .val-tag {
    background: rgba(107,124,85,0.12);
    border-color: rgba(107,124,85,0.35);
  }

  /* Bottom accent */
  .val-accent {
    position: absolute; bottom: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(to right, var(--olive), var(--steel));
    transform: scaleX(0); transform-origin: left;
    transition: transform 0.45s cubic-bezier(0.4,0,0.2,1);
  }
  .val-card:hover .val-accent { transform: scaleX(1); }

  /* CTA strip */
  .val-cta-strip {
    display: flex; align-items: center;
    justify-content: space-between; gap: 24px; flex-wrap: wrap;
    padding: 32px 40px;
    border: 1px solid var(--border);
    background: var(--bg-card);
  }
  .val-cta-text { display: flex; flex-direction: column; gap: 5px; }
  .val-cta-label {
    font-family: 'DM Serif Display', serif;
    font-size: 1.1rem; font-weight: 400;
    color: var(--text); letter-spacing: -0.01em;
  }
  .val-cta-sub {
    font-size: 0.72rem; color: var(--muted); font-weight: 300;
  }
  .val-cta-actions { display: flex; gap: 10px; align-items: center; flex-wrap: wrap; }

  .val-cta-btn {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 11px 26px; font-size: 0.68rem; font-weight: 600;
    letter-spacing: 0.15em; text-transform: uppercase;
    text-decoration: none;
    transition: background 0.25s, box-shadow 0.25s, gap 0.25s, border-color 0.25s, color 0.25s;
  }
  .val-cta-primary {
    background: var(--text); color: var(--bg); border: 1px solid var(--text);
  }
  .val-cta-primary:hover {
    background: var(--steel); border-color: var(--steel); color: var(--white);
    box-shadow: 0 4px 20px rgba(74,98,120,0.3); gap: 12px;
  }
  .val-cta-ghost {
    background: transparent; color: var(--muted); border: 1px solid var(--border);
  }
  .val-cta-ghost:hover { border-color: var(--olive); color: var(--olive-lt); }

  /* Reveal */
  [data-reveal] {
    opacity: 0; transform: translateY(22px);
    transition: opacity 0.75s cubic-bezier(0.22,1,0.36,1), transform 0.75s cubic-bezier(0.22,1,0.36,1);
  }
  [data-reveal].visible { opacity: 1; transform: translateY(0); }
  [data-delay="0"] { transition-delay: 0.00s; }
  [data-delay="1"] { transition-delay: 0.07s; }
  [data-delay="2"] { transition-delay: 0.14s; }
  [data-delay="3"] { transition-delay: 0.21s; }
  [data-delay="4"] { transition-delay: 0.28s; }
  [data-delay="5"] { transition-delay: 0.35s; }
</style>

<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600&family=DM+Serif+Display&display=swap" rel="stylesheet"/>

<script>
(function () {
  const obs = new IntersectionObserver(
    entries => entries.forEach(e => {
      if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target); }
    }), { threshold: 0.06, rootMargin: '0px 0px -30px 0px' }
  );
  document.querySelectorAll('[data-reveal]').forEach(el => obs.observe(el));
})();
</script>